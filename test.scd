/* sync text via network */

// Hannes here
// Alex is also here
// SU
// u?????????????????t?????????????????????????f????????????????????8????????????????????? ???????????????t?????????e???????????s???????????????t????????????????
// dennis here
// Ben here
// Test von Luis?
// valentin here
// Bruno is here


(
Ndef(\sinfm,{
	SinOsc.ar(440 + SinOsc.kr(5, mul:40), mul: 0.2)!2;
}).play;
)

s.meter;

(
Ndef(\err, {
	Blip.ar(60, SinOsc.kr(2/5, [0, 1], mul: 120).abs, mul: 0.1);
}).play;
)

(
Ndef(\err2, { // overtone singing
	Blip.ar(110, Blip.kr(1/25, [0, 1], mul: 10).abs, mul: 0.1);
}).play;
);



(
Ndef(\smthg, {
				Pan2.ar(SinOsc.ar(440));
}).play;
);
Ndef(\smthg).stop;


Ndef(\noise, {BrownNoise.ar(0.1)}).play
Ndef(\noise).stop;
(
Ndef(\ghrr, {
	Saw.ar((SinOsc.ar(2, 1) * 30), 0.1);
}).play;
)
Ndef.dictFor(s).do(_.end(10));

OSCRouterClient.groups;
OSCRouterClient.groups['hyperIMM'].peers;
OSCRouterClient.groups['hyperIMM'].peers.sort;
OSCRouterClient.groups['hyperIMM'].peers.size;

